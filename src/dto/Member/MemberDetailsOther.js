class MemberDetailsOther {
  constructor({
    id,
    risk,
    questions,
    retirement,
    retirement_money,
    work_with_advisor,
    real_id,
  }) {
    this.id = id
    this.risk = risk
    this.questions = questions
    this.retirement = retirement
    this.retirementMoney = retirement_money
    this.workWithAdvisor = work_with_advisor
    this.realId = real_id
  }
}

export { MemberDetailsOther }
